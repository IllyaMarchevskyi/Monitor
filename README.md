## Встановлення python на WINDOWS
1) Завантажте файл **install_python.ps1** з папки **scripts**
2) В деристорії де знаходиться файл запустіть **PowerShell**
3) Запустіть наступну команду
```
powershell -ExecutionPolicy Bypass -File .\install_python.ps1
```

## Встановлення arduino на WINDOWS
1) Завантажте файл **install_arduino.ps1** з папки **scripts**
2) В деристорії де знаходиться файл запустіть **PowerShell**
3) Запустіть наступну команду
```
powershell -ExecutionPolicy Bypass -File .\install_arduino.ps1
```
